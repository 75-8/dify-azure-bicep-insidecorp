# Phase 7: Deploy

Prepare or execute deployment after user confirmation.

- Use [deployment.md](../deployment.md).
- Prefer preview commands (`what-if` or `terraform plan`) before applying changes.
- Do not deploy destructive or cost-incurring changes without explicit confirmation.
- Capture outputs and post-deployment verification steps.
