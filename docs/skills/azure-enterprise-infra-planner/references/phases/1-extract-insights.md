# Phase 1: Extract Insights

Capture workload intent, current environment, constraints, and assumptions.

- Ask only for missing information that blocks planning.
- Use `insights_get` when available to understand existing Azure resources.
- Record subscriptions, regions, compliance needs, network boundaries, identities, dependencies, RTO/RPO, and budget constraints.
- Write assumptions clearly so the user can approve or correct them.
